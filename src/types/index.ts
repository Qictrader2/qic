// Domain types — will be populated as features are built out
// Shared types (api, trade, wallet, etc.) are synced from web via scripts/sync-from-web.sh
export type { AuthUser, UserRole } from "@/src/store/auth-store"
